import { expect } from 'chai';

import Person from '../../src/1.basic';

describe('Basic', function() {
	xit('Should exist', function() {

	});

	xit('Should be able to create a new instance', function() {

	});

	xit('Should be able to initialize the instance fields', function() {

	});

	xit('Should be able to retrieve the full name', function() {

	});
});
