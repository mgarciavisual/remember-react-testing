import { expect } from 'chai';
import sinon from 'sinon';

import delayedRemove from '../../src/3.fake-timer';

describe('Fake timer', function() {
	xit('Should exist', function() {

	});

	xit('Should remove the last element after 50ms', function() {

	});

	xit('Should remove the last element after 1500ms', function() {
	
	});

	xit('Should remove the last element after default timeout value', function() {
	
	});
});