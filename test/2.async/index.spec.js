import { expect } from 'chai';

import sut from '../../src/2.async';

describe('Async', function() {
	xit('Should exist', function() {

	});

	xit('Should remove the last element after 50ms', function(done) {

	});

	xit('Should remove the last element after 1500ms', function(done) {
	
	});

	xit('Should remove the last element after default timeout value', function(done) {
	
	});
});