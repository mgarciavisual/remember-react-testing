import { expect } from 'chai';
import sinon from 'sinon';

import getPostContent from '../../src/6.fake-server';

describe('Fake Server', function() {
	xit('Should get the post with a given id', function(done) {
		
	});

	xit('Should get the post with a given id (return promise)', function() {

	});

	xit('Should get empty body if something goes wrong', function() {

	});

	xit('Should get the post with a given id (advanced)', function() {
	
	});
});