import { expect } from 'chai';
import sinon from 'sinon';

import openWindow from '../../src/5.stub';

describe('Stub', function() {
	xit('Should exist', function() {

	});

	xit('Should open a URL with provided URL and title', function() {

	});

	xit('Should open a URL with the default title', function() {

	});

	xit('Should do nothing if no URL provided', function() {

	});
});